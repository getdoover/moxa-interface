[project]
name = "sample"
version = "0.1.0"
description = "Moxa ioLogik E1212 simulator for local testing"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
doover-app-run = "main:main"
